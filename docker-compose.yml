# Docker reste optionnel : l'application se lance aussi sans, voir le README.
services:
  app:
    build: .
    env_file: .env
    volumes:
      - ./uploads:/app/uploads
      - ./outputs:/app/outputs
      - ./temp:/app/temp
    ports:
      - "8000:8000"
